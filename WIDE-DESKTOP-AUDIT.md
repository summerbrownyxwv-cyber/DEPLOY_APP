# Wide Desktop 审计

## 1. 修改前问题

全站主容器上限1440px，实际视觉内容还需扣除内边距；2560px下两侧空间占比偏大。服务展示为自写分页，每组固定4项、2×2排列，JS中的页码、按钮、滑动和键盘步长也依赖固定分组。加盟产品占30%容器宽、发展历程约3项，不会随宽屏增加展示量。新闻为双行不等宽拼图，动画步长原先假定整组等于视口。Footer内部980px上限、AI舞台736px上限让大屏比例偏小。

扫描覆盖Layout/Container/Grid/Card/Carousel/Image/Typography/Section及其最终层叠覆盖，而非仅基础样式。未使用Swiper。

## 2. 文件

- `website-code/prototype/wide-desktop.css`：独立宽屏层。
- `website-code/prototype/index.html`：仅新增上述样式引用。
- `website-code/prototype/stores-page.js`：宽屏分组、页码、按钮、键盘、滑动及断点恢复。
- `website-code/prototype/about-page.js`：仅宽屏按实际新闻组宽度计算动画步长。
- `scripts/check-wide-desktop.cjs`：断点边界、资源不变、视频cover及分页算术检查。

没有修改原Desktop CSS、品牌色、字体文件、图片资源、文案或内容项数量。

## 3. 断点与容器

所有新增CSS仅在`min-width:1920px`与`min-width:2560px`生效。未更改1280–1919px的列数，Desktop不变原则优先于示例列数。

视觉区上限1920px，文字/公告/门店信息/申请表区域保持1440px阅读上限，局部介绍约640px。没有提升全局字体或section padding。首页Display在两档为72/80px，正文继承原16px系统。AI舞台上限960/1120px，仍受视口高度限制。

3840px下视觉内容仍以1920px为上限，不无限放大；全幅背景保留。此为用户允许的视觉区上限策略，不承诺消除所有两侧留白。

## 4. 卡项及图片规则

| 范围 | 服务每页 | 实际10项分页 | 历程可见列 |
| --- | --- | --- | --- |
| <1920 | 原4项 | 4/4/2，共3页 | 原规则 |
| 1920–2559 | 5项 | 5/5，共2页 | 5 |
| ≥2560 | 6项 | 6/4，共2页 | 6 |

服务图片保持215/307比例、cover，宽度clamp并限制在卡片内；2560实测约249px宽。宽屏介绍区与整行图片分开，选中信息按两列阅读区排列。断点改变时移动原DOM卡片而非复制，保留事件绑定，重算页码并恢复Desktop的4项分组。

加盟图库/产品实际各4项：完整展示4项，不复制或补空卡。新闻保留双行拼图，宽屏约1.5组、2560以上约2组可见；循环距离以实际组宽计算，原Desktop算法保持。Footer保留实际三列结构，只扩宽内部容器和列间距。

## 5. 追加视频要求

≥2560px门店Hero视频取消1920px媒体上限，宽高填满容器、`object-fit:cover`。保持比例，允许必要边缘裁切。普通Desktop仍使用原视频规则。

## 6. 已完成验证

18路由（首页、3个品牌页、5个门店页、AI、加盟及申请、6个关于/合作页）×4尺寸，共72组初轮检查：

| 尺寸 | 整页横向溢出 | 服务首屏 | 视觉容器 |
| --- | --- | --- | --- |
| 1440×900 | 未检出 | 4项，1/3 | 原宽度 |
| 1920×1080 | 未检出 | 5项，1/2 | 约1905px（滚动条占15px） |
| 2560×1440 | 未检出 | 6项，1/2 | 1920px |
| 3840×2160 | 未检出 | 6项，1/2 | 1920px |

2560服务卡项截图已检查；点击第二页显示6–9号卡片、2/2、下一页禁用。缩回1440恢复4项及2/3，未丢失内容。

1440的18路由关键元素修改前后比较：16路由一致；两个门店详情页出现4px高度差，其余采样属性一致，可能与图片加载时序有关，但尚未通过同一文档样式隔离排除，不能宣称像素级零差异。

## 7. 待复验与限制

最终浏览器样式隔离检查被授权服务额度限制阻止。最新视频cover、服务介绍两列及4项图库修正尚未进行最终截图复验；1920/2560跨断点键盘与拖动仍需补测。72组为此前版本的几何检查，不是全部交互状态或像素级验收。静态脚本不替代浏览器验证。

恢复工具授权后，应补验1440/1919样式隔离、2560/3840视频边缘及主体裁切、新闻循环接缝和减少动态效果模式，再完成最终视觉验收。
