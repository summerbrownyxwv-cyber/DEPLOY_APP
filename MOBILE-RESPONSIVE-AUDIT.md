# Mobile Responsive Audit

## 本次 Mobile Responsive System 修复（最新结果）

以下结果优先于后文历史记录。先只读审计、经用户确认后实施；沿用 Ponytail 最小修改与 UI/UX 可读性原则，没有重做品牌视觉。

### 问题与修改文件

- `website-code/prototype/mobile-responsive.css`：历史改纵向；新闻单卡原生横滑；合作入口高度220–260px；Hero手机比例；表格16px并局部横滑；768–1023.98px单独限制媒体尺寸与适合双列的卡片。修复首页比例与最小高度反推384px宽的问题，明确宽度100%。
- `website-code/prototype/about-page.js`：仅低于1024px取消新闻自动循环，按单卡宽加gap推进；Desktop原算法保留。
- `website-code/prototype/brand-figma.js`：仅低于1024px关闭历程横向位移及横滑切换，年份定位对应卡片，上下键进行纵向导航；Desktop左右切换保留。
- 本报告与既有移动静态测试；dist由构建生成。

### 规则与特殊处理

全部新增CSS仍在`max-width:1023.98px`内。手机页边距20px，平板24px；沿用Display36–42、H1 32、H2 26–30、H3 20、正文16、Caption13px的rem/clamp体系。

首页和品牌Hero手机以3:4构图，但允许内容撑高，避免裁字；平板4:3。门店视频4:3并cover，可能裁切边缘；证书、报告不裁切。合作入口使用220–260px明确高度，16:9为参考比例，狭窄屏优先满足可读高度而非同时强制两个冲突尺寸。新闻16:9；平板新闻限宽576px。平板媒体限宽576px，部分产业、服务、合作入口卡两列；表单和Footer维持单列。

历史改为纵向后会增加该节总长度，这是保留全部内容的正常结果；新闻不再四卡纵向堆叠后整组横移。隐藏的仅为原循环所需的重复克隆，原12条资讯均保留。

### 实测

18路由×375×667、375×812、390×844、430×932、768×1024，共90组复测，整页横向溢出0。初轮两项375px首页溢出已定位并修复。

| 视口 | 新闻区高度 | 合作入口高度 |
|---|---:|---:|
|375×667 / 375×812|180px|225px|
|390×844|188px|234px|
|430×932|211px|258px|
|768×1024|324px（原1664px）|260px（原487px）|

390px实测新闻ArrowRight推进351px（335px卡宽+16pxgap），截图确认第二张卡及入口文字完整。1024、1440、2560各18路由，共54组同文档启用/禁用Mobile CSS比较，所有body后代的宽高、字号、padding、margin、display、grid列、gap、颜色、aspect-ratio差异为0。这证明CSS隔离，不代表所有动效的逐像素等价。

### 性能与验收边界

沿用已有WebP/srcset、首图优先、非首屏lazy和图片尺寸占位；手机新闻取消无限动画，采用原生滚动。未修改资源文件或Desktop加载策略。冷启动网络、真实设备LCP/CLS和软键盘未进行定量测试，不宣称首屏测速通过；几何检查不是全部交互状态人工验收。

## 以下为上一轮基础适配记录

## 范围与Desktop保护

本轮只新增Mobile覆盖，不修改已有页面CSS、JavaScript、图片、字体、文案或品牌色。
需求中的“Desktop ≥1024px”与“max-width:1024px”存在边界重叠，采用更严格的 `@media (max-width:1023.98px)`，保护1024px整宽Desktop。所有新规则（包括tokens）均在该条件内。没有通过改变根字号缩放页面，没有新增全局overflow隐藏。

## 修改文件

- `website-code/prototype/mobile-responsive.css`：集中维护本轮全部移动端覆盖。
- `website-code/prototype/index.html`：仅在现有CSS之后加载上面的文件。
- `scripts/check-mobile-responsive.cjs`：检查所有新规则被Mobile media包围、无图片字体替换、关键token和组件规则存在。
- `MOBILE-RESPONSIVE-AUDIT.md`：本报告。
- `dist`：通过既有构建生成对应文件，不手动修改。

## 扫描发现及原因

扫描了首页、品牌主页面及旧历史/产品页、门店主页面/服务/详情、AI、加盟及申请、关于我们及三种合作表单的CSS与渲染结构。重点检查固定宽高、min-height、字号、内外边距、网格/弹性方向及图片比例。

1. Footer手机仍保留双列；菜单使用剩余高度居中，产生过多留白。
2. 菜单隐藏序号后，窄序号列与直接文本组合会造成逐字换行；改为flex文本行。
3. 门店优势缩略图三列、新闻卡片两列，中文内容过窄；改为单列。
4. 部分门店卡图、门店详情与品牌图片继承竖图高度；以4:3限定常规卡图。
5. 768–1023px仍有两列图文和表单；统一覆盖，不改变1024px以上。
6. 店型图文重复section padding/margin、公告最小高度、Footer大上下间距，放大移动阅读长度；改为明确的移动间距。

## Mobile Tokens

| Token | 规则 |
|---|---|
| `--m-display` | 36–42px clamp |
| `--m-h1` | 32px |
| `--m-h2` | 26–30px clamp |
| `--m-h3` | 20px |
| `--m-body` | 16px，正文行高1.75 |
| `--m-caption` | 13px；特殊注释维持12–14px |
| `--m-page` | 20px |
| `--m-section` | 56–72px |
| `--m-gap` | 16–20px |

移动断点内映射到原 `--x-` 语义token，避免页面继续继承Desktop值。局部卡片标题、数字、标签保留语义层级，不把统计数字强制变成正文。

## 组件与页面处理

- Header：64px，Logo图宽最多120px；保留现有Logo裁切方式，菜单按钮48×44px。
- Drawer：24px padding，顶部44px头部；菜单28–32px、1.85行高，列表按内容高度排列。按钮自动宽、48px高；保留关闭、焦点与Escape逻辑。
- Footer：单列，DOM顺序即Logo → 语言/投诉 → 联系方式 → 快捷导航 → 加入我们。文字可换行，底部保留安全区与浮层空间。
- 首页：图文介绍、产业卡片、门店区、AI与合作入口单列；Hero保留沉浸视觉，正文不再继承桌面大留白。
- 品牌：介绍/集团/产品/终端/荣誉单列；商业模式三张封面纵向排列，展开详情仍使用原交互，内部改为上下结构；历史使用一张卡片宽度的局部滑动，旧历史页内部也为单列。
- 门店：优势卡片、店型、服务卡片、详情图文均单列。常规图片4:3；有figcaption的图片不把说明挤出容器。搜索框回到手机满宽，输入16px。表格继续使用原局部横向滚动，不压缩字体。
- AI：设备与标题独立排列，内容矩阵/价值/场景单列；设备、证书、报告采用contain保持内容完整。报告图片限定24rem、最高65svh，不机械裁掉报告文字。
- 加盟：店型、输出、图文、表单单列；产品和总部图库仍可局部滑动，手机卡图4:3；流程手机折叠、平板竖向文字加当前图片，复用原逻辑。
- 关于我们：公告、招聘/商务/海外入口单列；新闻每组内部单列16:9卡片，保留原新闻滑动行为，不删内容。
- 表单：单列，输入≥44px高且16px字号，提交按钮满宽；不改校验和后台事实。

## Desktop不变证据

1. 新CSS的唯一顶层规则为1023.98px以下media；资源引用未新增；已有Desktop规则未编辑。
2. 浏览器在1024×900及1440×900对18条路由（36组合）进行同文档隔离比较：启用与禁用新增CSS，检查每个body后代元素的字号、行高、颜色、背景、宽高、padding、margin、display、网格列、position、aspect-ratio，差异均为0。比较后恢复启用CSS。
3. 最初跨加载前后比较包含Reveal class变化及图片加载导致的4px差异，不把它当作样式改变或完全一致证明；以上同文档隔离检查避免了异步加载干扰。
4. 静态检查命令：`node scripts/check-mobile-responsive.cjs`。这不等同于跨浏览器逐像素截图证明。

## 实际Mobile验证

18路由：`/`、`/brand/about`、`/brand/history`、`/brand/products`、`/stores/standard`、`/stores/black-gold`、`/stores/services`、`/stores/standard/detail`、`/stores/black-gold/detail`、`/ai`、`/franchise`、`/franchise/apply`、`/about/culture`、`/about/news`、`/about/join`、`/about/careers`、`/about/business`、`/about/overseas`。

| 视口 | 组合数 | 页面横向溢出 | 隐藏容器内标题/正文/按钮尺寸裁切 | 普通figure/资讯卡高于0.9视口 |
|---|---:|---|---|---|
|375×812|18|无|未检出|未检出|
|390×844|18|无|未检出|未检出|
|430×932|18|无|未检出|未检出|

截图抽查了390px手机菜单（六行横排、按钮完整，无中间大空白）与375px品牌商业模式展开（上下构图，无横向溢出，内容高约690px）。保留截图人工检查与几何检查的区别，不宣称54组合每一屏都已人工验收。

追加768×1024的18路由检查，均无整页横向溢出。加盟产品按钮的选中态单独补齐100%卡宽，避免旧的82%选中态宽度覆盖移动单卡规则。

## 限制与建议人工检查

- 当前验证为本地浏览器CSS像素模拟，未使用真实iOS/Android；请再确认触控板/触摸、系统字体回退与软键盘行为。
- 全部图片焦点、展开后的所有证书/新闻/服务状态、200%缩放、慢网络CLS仍未穷尽；证书与AI报告明确优先完整展示。
- 不承诺所有长页面变短：内容完整保留，卡片单列后总阅读长度可能增加，但单张图片有明确比例。
- 本轮不调整Desktop现存问题，也不修改之前版本的字体资源或动效系统。
