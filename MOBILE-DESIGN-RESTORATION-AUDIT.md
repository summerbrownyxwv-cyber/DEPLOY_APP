# MOBILE DESIGN RESTORATION AUDIT

日期：2026-09-22  
阶段：第一阶段，仅分析；本轮未修改网站 CSS、JS、HTML、图片或构建产物。

## 1. 视觉基线与证据边界

以本次用户提供的 13 项批注及配套 earlier mobile 截图为视觉基线，以本地 `website-code/prototype` 当前源码和用户标注的 current 截图为问题证据。未取得可唯一定位的 earlier Git 提交或完整 Figma 文件，因此不宣称逐像素还原，也不把任意历史提交当成正确版本。

本轮完成截图对照和源码审计，未执行修改后的浏览器测试。此前“没有横向溢出”的测试不能证明视觉还原度合格。参考截图中的浏览器栏、系统状态栏和图片未加载形成的空白，不属于要复制的网站设计。

## 2. 核心结论

当前最大问题并非所有模块都需要更小，而是原有的主次、并置、拼版及横向浏览关系被统一尺寸规则覆盖：

- 产品区从“小图并排—大图—大图—小图并排”变为等宽单列，失去重点。
- 终端服务、AI 场景失去主图与辅图之间的比例关系；标题和 CTA 被强制上下拆开。
- 服务卡项从每页 2×2 变为四张纵向排列，虽然 JS 仍按四项分页，却已失去原卡项构图。
- 历程从横向浏览变为全部纵向展开；年份换行，增加页面长度，改变交互叙事。
- 新闻两行组合被拆成单卡，自动滚动被移动端分支取消。
- 统一段距、图像比例、标题规则压过组件原有设计。通用规则应只兜底，不应取代组件构图。

这些是错误的重构，需要定点恢复；仪器对齐、滑动暗示缺失、局部字号与页脚展开过长则是实际局部问题。

## 3. 13 项批注恢复清单

| 批注 | 当前问题 / 分类 | 恢复目标 | 实施时验收 |
|---|---|---|---|
| 1 首页 AI | 仪器面板与轨道中心不一致；真实对齐问题 | 以面板可见区域中心定位，不以含右侧配件的整张图片中心定位；仅校正首页手机端位置 | 三种宽度及旋转过程中中心一致，不裁掉主体，不影响 AI 页或 Desktop |
| 2 Footer | 联系方式、导航、加入我们全部展开，页面尾部过长；交互调整 | 保留深色品牌视觉、内容和顺序；三个分组改为带细分隔线与箭头的折叠行，默认收起 | 点击、键盘及展开状态可用；≥44px 点击区；Desktop 不出现折叠逻辑 |
| 3 品牌 Hero | 标志、标题、正文组合占比过大；局部尺寸问题 | 局部缩小整个内容组合的字号、标志尺寸与内部间隔，保留居中叠图和文案 | 小屏完整可读，不挤出首屏构图；不通过删字或整体 scale 修复 |
| 4 企业历程 | 年份多行、卡片全部纵向展开；错误重构 | 恢复单行年份导航、横向主卡与下一张露出、点年份与滑动联动 | 年份、选中卡、键盘、手势一致；局部横滑不导致页面横溢出 |
| 5 明星产品 | 六图全部单列、CTA 落在标题下；错误重构 | 恢复温/燃并排，艾灸器与精华帖各占通栏，萃/植并排；标题左、CTA 右 | 保留图片顺序、大小对比与原素材；不强制所有图片 4:3 |
| 6 终端服务 | 辅图单列、标题与 CTA 拆开；错误重构 | 首张门店图和说明通栏，下面两张辅图并排；品牌名左、CTA 右 | 主辅关系与截图一致，文字不溢出，全部内容保留 |
| 7 社会责任 | “03 社会责任”靠左；局部对齐问题 | 仅此标题组移到右侧，不改其他荣誉组 | 数字与标题共同右对齐，不倒序、不误伤其他标题 |
| 8 寻艾竞争力 | 三张缩略卡变成大图列表；错误重构 | 恢复上方三张竖向缩略卡并排，下方大主图；保留选择切换 | 三项文字可读、点击切换正常，不用新列表取代原组件 |
| 9 服务卡项 | 单列图片破坏两行两列卡片结构；错误重构 | 恢复每页 2×2、竖向服务图片、标题与分隔说明并置、页码在下 | 翻页、滑动、卡片选择与详情仍可用；保留真实图片，不复制参考中的加载空白 |
| 10 AI 报告 | 报告被 24rem / 65svh 限高，视觉重心缩小；错误尺寸约束 | 恢复大幅竖屏报告横滑，右侧露出下一份报告 | 屏幕内容不裁切、不拉伸；保持原图比例，横滑可发现 |
| 11 AI 场景 | 两图变成上下横图、CTA 下移；错误重构 | 恢复约 1:1.25 的双竖图、标题左 CTA 右，底部三类场景同行 | 构图、人物与设备裁切合理，底部标签可读 |
| 12 加盟产品 | 卡片宽度 100%，下一张不可见；真实交互暗示问题 | 恢复约 82%–88% 主卡宽度与相邻卡露出；保留原滑动/选择机制 | 首屏可识别下一张，首尾滑动不截断；图片正常加载 |
| 13 品牌资讯 | 两行组合变成单张横图，连续滚动停止；错误重构 | 恢复两行组合的竖向新闻卡及横向连续滚动，标题仍右对齐 | 自动滚动与触摸浏览协调；聚焦/离屏暂停；减少动态偏好下仍可手动访问全部内容 |

## 4. 源码根因与拟修改位置

路径均相对项目根目录。

### `website-code/prototype/mobile-responsive.css`

该文件在页面组件 CSS 和 `site-consistency.css` 之后载入，当前包含以下直接覆盖：

- `.xf-products`、`.xf-terminal` 等共享 `grid-template-columns:minmax(0,1fr)`。
- `.xf-products figure:nth-child(n)` 重置 `grid-column/grid-row`，抹掉 featured 图的跨列。
- `.xf-subheading`、`.xs-service-heading`、`.xa-scenario-heading` 共用 `flex-direction:column`，拆开标题、描述、CTA。
- `.xs-strength-thumbs`、`.xs-service-group`、`.xa-scenario-photos` 单列化。
- `.xs-strength-photo`、`.xs-card-photo`、产品/终端/场景图片共用 4:3，改变竖图和拼版比例。
- `.xf-history-track` 纵向、`transform:none!important`；年份 `flex-wrap:wrap`。
- `.xa-report-grid img` 限高；`.xj-product` 的 `flex-basis:100%` 消除露出。
- `.xo-news-track` 单卡流、`.xo-news-group{display:contents}`、隐藏循环副本且禁用 transform。

方案：定点收窄/替换这些覆盖，保留真正有效的容器边界、表单字号、点击区、菜单和局部溢出修复。不能直接删除整个移动端文件，否则会恢复其他已解决的 bug。

### `website-code/prototype/brand-figma.js`

历程 `positionYear` 在 ≤1023.98px 强制无位移，年份点击改成纵向 `scrollIntoView`，方向键改成上下键，手机触摸切换提前返回。恢复横向交互必须同时调整这些手机分支，不能只改 CSS。

### `website-code/prototype/about-page.js`

新闻 `measure()` 在 ≤1023.98px 取消动画并提前返回。原有循环、离屏/聚焦暂停与减少动态偏好分支仍在，可复用，不需要新增轮播库。恢复分组 CSS 的同时，手机分支的步长必须与实际分组宽度一致。

### `website-code/prototype/index.html` / `script.js`

Footer 目前是三个 `section/nav`，标题为 `h2`，没有折叠状态。后续优先在手机条件下增强现有分组标题与内容关系，不复制另一套页脚，不增加依赖。回到 Desktop 时必须解除折叠隐藏和手机专用状态，保留原导航语义。

### 仅供基线核对，优先不改的文件

- `brand-figma.css` 仍保留产品两列跨列、终端主图通栏、历程主卡 88% 等手机规则。
- `stores-page.css` 仍保留三缩略卡、2×2 服务卡结构及竖图比例。
- `ai-page.css` 仍保留报告 85% 卡宽、场景双图 1:1.25 等手机规则。
- `about-page.css` 仍保留新闻两列、两行组合。
- `site-consistency.css` 还可能覆盖这些规则，必须按最终级联验证，不能仅撤掉一条规则就认定恢复完成。
- `stores-page.js` 当前普通屏每页四项，Wide 五项、4K 六项。手机恢复 2×2 不应改动宽屏分组算法。

## 5. 页面级判断

| 页面 | 判断 | 本次边界 |
|---|---|---|
| 首页 `/` | 仪器对齐为真实 bug，Footer 为明确新增手机交互要求 | 先处理批注 1、2；其他首页模块没有足够旧版证据，不推测回退 |
| 品牌实力 `/brand/about` | 过度工程化最集中：历程、产品拼版、终端主辅图与 CTA | 处理批注 3–7，恢复组件自身逻辑 |
| 艾灸馆 `/stores/standard` | 竞争力与服务卡项被错误单列化 | 处理批注 8、9；共享组件关联路由一起检查 |
| AI `/ai` | 报告限高与场景单列改变原构图 | 处理批注 10、11，不改设备/报告素材 |
| 加盟 `/franchise` | 产品横滑暗示消失 | 处理批注 12，不擅自改变流程与其他模块 |
| 关于我们 `/about/culture` | 新闻分组与滚动被替换 | 处理批注 13，保留公告及合作入口内容 |
| 人才招聘 `/about/careers` | 当前为共享单列表单；没有 earlier 对照证据证明布局错误 | 保留字段与表单结构，检查页脚、边界、可读性，不推测回退 |
| 商务合作 `/about/business` | 同上 | 保留现有表单逻辑，做回归检查 |
| 海外合作 `/about/overseas` | 同上 | 保留现有表单逻辑，做回归检查 |

品牌历史/产品子路由、门店黑金/服务/详情、加盟申请、关于新闻/加入我们等共享路由也应列入回归；共享组件受影响不等于允许重设计其余内容。

## 6. Mobile Design Guardrails

1. 不变：Logo、品牌颜色、字体家族、文案、素材、模块顺序、主辅图关系、卡片逻辑、标题与 CTA 关系。
2. 不把“局部可横滑”误判为“整页横向溢出”；横滑是参考设计的一部分。
3. 不统一所有图片比例：报告保留完整设备比例，服务图保留竖向构图，产品保留大小对比，新闻保留两行组合。
4. 仅允许局部 padding、字号、行高、图像比例/高度和点击区域微调；不再引入跨组件一刀切规则。
5. 本轮手机修复拟限定 `<768px`，匹配提供的手机证据和原组件手机断点；768–1023px 仅回归、不主动重排。Desktop ≥1024px 与 Wide 层保持不变。
6. 手机交互需要响应宽度切换：进入/离开断点时清理临时状态，不能把折叠隐藏或轮播位置泄漏到 Desktop。
7. 按钮/折叠触发器保留键盘访问、可见焦点、状态语义；新闻动效尊重减少动态设置，不阻碍页面纵向滚动。
8. 沿用原生 CSS/现有交互，不新增依赖、不批量格式化、不回滚无关改动。Ponytail 的最小改动原则用于限制实现范围，而不是缩减用户的恢复要求。

## 7. 实施与验证计划（尚未执行）

1. 先保存手机和 Desktop 的修改前截图及关键计算样式。
2. 按 13 项逐组件恢复；先处理导致错误构图的覆盖，再补局部对齐、折叠及滑动暗示。
3. 在 375×812、390×844、430×932 各检查参考构图、横滑露出、标题/CTA、图片裁切、全部文字可读性。
4. 检查 Footer 三组折叠；历程年份点击/左右切换；服务分页/选择/详情；加盟卡片滑动；新闻自动滚动/拖动/键盘/暂停及减少动态模式。
5. 页面根节点不得横向溢出；允许组件内部预期横滚。检查加载后高度、按钮边界、首尾卡可达性及设备中心对齐。
6. 768×1024 回归 Tablet。1024、1440、1920、2560、3840 宽度对比同路由修改前后截图及计算样式，确保 Desktop 和 Wide 无变化；动态截图在相同播放状态下比较。
7. 构建后核对源码与预览产物一致，再输出 `MOBILE-DESIGN-RESTORATION-REPORT.md`，记录实测结果，不能用本审计代替完成报告。

## 8. 待确认与不确定性

- 旧截图只覆盖部分模块，未覆盖页面不能据此宣称已还原 Figma。
- 仪器面板中心需在实施时结合原图可见面板边界校准，CSS 的整图几何中心不是验收依据。
- 历程参考截图与现有年份内容可能不完全一致；只恢复排版交互，不补造年份/事件。
- Footer 折叠建议三个组默认收起、独立展开；参考 Apple 的交互，不复制其颜色、字体或内容。
- 新闻现有占位组仍属现有内容，不擅自删除或替换；恢复滚动后仍需全部可达。
- 旧图里服务卡、加盟产品的空白疑似图片加载未完成，仅恢复其布局，不恢复空白。

本轮文件变更：仅新增本审计文档。网站实现与最终恢复报告留待下一阶段。
