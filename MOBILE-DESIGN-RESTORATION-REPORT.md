# MOBILE DESIGN RESTORATION REPORT

完成日期：2026-09-23  
范围：按 13 项批注恢复手机端视觉关系与交互。未更换图片、Logo、字体家族、品牌颜色或文案；未增删业务内容。

## 1. 恢复结果

| 批注 | 已完成的手机端恢复 |
|---|---|
| 1 首页 AI | 校正设备位置，以图片左侧面板中心对齐轨道，而非包含右侧配件的整图中心；仅影响首页手机模块 |
| 2 Footer | 联系方式、快捷导航、加入我们改为独立折叠组，默认收起；保留深色品牌视觉，支持键盘与展开状态语义 |
| 3 品牌 Hero | 局部缩小标志、标题、正文和内部间距；保留居中叠图、完整文案及原有入场动效 |
| 4 企业历程 | 恢复单行年份、88% 宽主卡及下一张露出；年份点击、左右方向键与横向手势联动 |
| 5 明星产品 | 恢复“温/燃并排—艾灸器通栏—精华帖通栏—萃/植并排”；恢复标题左、CTA 右及产品名称字号 |
| 6 终端服务 | 首张门店图及说明通栏，两张辅助照片并排；标题与 CTA 恢复同排 |
| 7 社会责任 | 仅“03 社会责任”标题组右对齐，不改变其他荣誉组 |
| 8 门店竞争力 | 三张竖向缩略卡并排，下面展示大主图；原选择切换保留，未调整内容顺序 |
| 9 服务卡项 | 恢复每页 2×2 卡片、竖向图片、并置标题与分隔说明、下方分页；末页按实际剩余两项显示 |
| 10 AI 报告 | 移除手机报告的 24rem / 65svh 限高，恢复 85% 宽竖屏报告与下一张露出；完整展示报告图 |
| 11 AI 场景 | 恢复 1:1.25 双竖图、左标题右 CTA，底部三类场景保持同行 |
| 12 加盟产品 | 恢复 82% 主卡宽度，下一卡局部可见；430px 测试中露出约 47.5px |
| 13 品牌资讯 | 恢复两行两列的新闻组合及连续横向滚动；保留离屏/聚焦暂停、键盘浏览与减少动态模式 |

只恢复参考中的设计逻辑，不复制参考图片未加载造成的空白。既有门店、历史年份、新闻占位内容全部保留。

## 2. 页面与修改范围

实际修改的运行时源码只有四个文件：

- `website-code/prototype/mobile-responsive.css`：新增手机专用恢复块，重建原拼版与图文比例；解除新闻 transform 的动画阻断。
- `website-code/prototype/brand-figma.js`：将手机历程恢复为横向交互，保留 Tablet 原纵向逻辑和 Desktop 原横向逻辑。
- `website-code/prototype/about-page.js`：仅解除手机新闻动画的提前返回，Tablet 仍维持原处理。
- `website-code/prototype/script.js`：手机断点下增强现有 Footer；离开断点还原原标题节点及全部内容显示。

其他交付：

- `scripts/check-mobile-restoration.cjs`：可重复执行的手机恢复边界与关键构图检查。
- `MOBILE-DESIGN-RESTORATION-AUDIT.md`：前置审计，保留其阶段性记录。
- 本报告。
- `dist/website-code/prototype/` 中对应四个文件由构建同步。

首页、品牌概览、门店、AI、加盟、关于我们按批注恢复。人才招聘、商务合作、海外合作仅共享手机 Footer 调整并做回归，没有推测性重排表单。Header 和 Menu 本次未修改。

## 3. Desktop / Tablet 保护证据

1. 恢复样式集中在 `@media(max-width:767.98px)` 内；没有修改 `wide-desktop.css`、基础组件 CSS 或品牌 Token。
2. Footer 增强仅在同一手机断点启动。实测由手机切到 1024px 后：折叠按钮为 0，隐藏内容为 0，标题恢复原文字节点。
3. 历程和新闻保留 768–1023.98px 的旧逻辑，Desktop 分支和宽屏服务卡分组算法保持原样。
4. 构建前对比旧 `dist` 与源码目录，只有上述四个运行时文件不同；`design-system` 无差异。
5. 使用旧构建和当前源码，在同一浏览器、相同视口及减少动态模式下，对 `main`、Header、Footer 及其后代逐节点比较：display、width、height、font-size、line-height、padding、margin、gap、grid-template-columns、aspect-ratio、object-fit。

| 视口 | 页面数 | 计算样式差异 |
|---|---:|---:|
| 768×900 | 9 | 0 |
| 1024×900 | 9 | 0 |
| 1440×900 | 9 | 0 |
| 1920×1440 | 9 | 0 |
| 2560×1440 | 9 | 0 |
| 3840×2160 | 9 | 0 |

9 页为首页、品牌概览、标准店、AI、加盟、企业文化、人才招聘、商务合作、海外合作。总计 54 组非手机端计算样式对照，全部一致。这是布局与排版级证明，不冒充逐像素动画帧比对。

## 4. 手机测试结果

| 尺寸 | 路由数 | 整页横向溢出 | 手机 Footer |
|---|---:|---:|---|
| 375×812 | 18 | 0 | 三组折叠存在 |
| 390×844 | 18 | 0 | 三组折叠存在 |
| 430×932 | 18 | 0 | 三组折叠存在 |

18 路由：`/`、`/brand/about`、`/brand/history`、`/brand/products`、`/stores/standard`、`/stores/black-gold`、`/stores/services`、`/stores/standard/detail`、`/stores/black-gold/detail`、`/ai`、`/franchise`、`/franchise/apply`、`/about/culture`、`/about/news`、`/about/join`、`/about/careers`、`/about/business`、`/about/overseas`。

每次检查都核对浏览器实际宽高，避免把未生效的模拟尺寸计为通过。局部轮播的 scrollWidth 大于自身宽度是预期设计，不等同整页溢出。

截图检查覆盖 Hero、历程、明星产品、终端服务、竞争力、服务卡项、AI 报告、AI 场景、首页仪器、Footer、加盟产品、新闻。选中区域未见明显错位、异常拉伸或 CTA 越界。375px 产品区为两列，服务区为两列，竞争力缩略图区为三列；报告和加盟产品均保留横向露出。

### 交互验证

- 历程：点击 2016 后按右方向键，选中 2017；卡轨位移正确，年份仍为单行。
- 社会责任：计算样式 `justify-self:end`。
- Footer：展开联系方式后内容可见；此前快捷导航独立展开正常；切至 Desktop 自动解除增强。
- 竞争力：点击缩略图后主图切换，缩略卡总数仍为 3。
- 服务卡项：翻至 `3 / 3`，下一页禁用，显示剩余两项；点击卡片后详情正常显示。
- 加盟：右方向键选择“温系列”，轨道滚动约 327.5px。
- AI 报告：右方向键使局部轨道滚动 375px。
- 新闻：正常模式 600ms 内位移从约 -12.47px 变为 -25.67px；离屏后动画暂停。
- 新闻减少动态模式：无运行中轨道动画，方向键仍可横向浏览，实测 scrollLeft 399px。
- Hero 正常动效模式：标题和正文入场完成后 opacity 均为 1，没有因缩小而隐藏文案。
- 最终测试页浏览器错误日志为空。

### 命令验证

- `node scripts/check-mobile-responsive.cjs`：通过。
- `node scripts/check-mobile-restoration.cjs`：通过。
- 三个修改的 JS 文件 `node --check`：通过。
- `npm run build`：将已验证源码同步到本地 dist；未执行线上发布。

## 5. 设计保护规则与人工确认

- 此次采用 Ponytail 的局部修复原则，复用现有组件、原生折叠状态及既有轮播，不新增依赖或通用组件体系。
- 以用户 earlier 截图为构图基线，不再把所有多列内容统一改成单列，也不把所有图片统一成横图。
- 无完整、可唯一定位的 earlier Git / Figma 基线，因此应由用户最终确认品牌节奏与视觉还原度；不宣称逐像素复刻。
- 当前验证为浏览器响应式模拟，不替代真机 Safari 的最终触摸手感验收。
- 历程参考图的年份与现有数据不完全一致，未编造年份；新闻占位文案、联系方式占位等既有内容未擅自修改。
- 保留未被本次批注涉及的页面结构和此前真实响应式修复。

## 6. 后续三项手机端批注修订

- 仅在 `max-width:767.98px` 生效；本节覆盖此前新闻双行和社会责任标题的处理说明。
- 品牌资讯：改为单行连续横向滚动，保留全部资讯与原卡片宽度；同步手机端轮播步长，避免按旧组宽循环。
- 企业历程：移除卡片强制最小高度，利用轨道拉伸统一十张卡片高度；保留内容完整，图像顶部与文案底部距离边框均为 21px（含 1px 边框）。
- 社会责任：保持标题组右对齐，仅将数字移至文字右侧，呈现“社会责任 03”。
- 本次修改：`website-code/prototype/mobile-responsive.css`、`website-code/prototype/about-page.js`、`scripts/check-mobile-restoration.cjs`，以及本报告；构建同步到 `dist`。
- 手机检查：375×812、390×844、430×932，两个页面共 6 组检查无页面横向溢出；历程卡片等高，资讯组内四张卡片位于同一行。正常动态模式下资讯轨道位移变化，确认滚动仍运行。
- 非手机对照：768、1024、1440、2560px 宽度下，品牌页与关于页共 8 组主要布局计算样式与本次修改前的 dist 基线完全一致。
- 自动检查：两项 Mobile 检查脚本与 about-page.js 语法检查通过。本轮未重新执行此前全部页面矩阵；真机触摸手感仍需人工验收。
